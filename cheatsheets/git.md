# Git Cheat Sheet

how to initialize a repo, add a file to staging area, committing, get the status of a repo, cloning, and anything that comes to mind. 

git config --global core.editor "notepad"
git config --global core.editor "code --wait"
